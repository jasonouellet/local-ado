# Copie en .env si tu veux, puis ajuste.
# Note: ne mets pas de secrets réels ici.

ADO_MOCK_HOST=0.0.0.0
ADO_MOCK_PORT=8080

ADO_MOCK_FIXTURES_PATH=fixtures/demo.json
ADO_MOCK_DATASET=demo

# none | pat | bearer
ADO_MOCK_AUTH_MODE=none
ADO_MOCK_TOKEN=local-dev-token

ADO_MOCK_STRICT_API_VERSION=false
ADO_MOCK_ALLOWED_API_VERSIONS=7.0,7.1-preview.1

# Pour simuler de la latence (tests de retry/backoff)
ADO_MOCK_ARTIFICIAL_LATENCY_MS=0
